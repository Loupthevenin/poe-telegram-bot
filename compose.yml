services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: poe-telegram-bot
    # restart: unless-stopped

    env_file:
      - .env

    environment:
      DATABASE_PATH: /app/data/poe_bot.db

    volumes:
      - ./data:/app/data
