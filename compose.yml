services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: poe-telegram-bot
    # restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./data:/app/data
